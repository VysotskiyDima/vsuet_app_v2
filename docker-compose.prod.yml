services:
  redis:
    image: redis:7-alpine
    container_name: vsuet_rating_v2-redis
    command: ["redis-server", "--save", "60", "1", "--dbfilename", "dump.rdb", "--dir", "/data"]
    volumes:
      - redis-data:/data
    restart: unless-stopped

  app:
    image: ghcr.io/vysotskiydima/vsuet_app_v2:${IMAGE_TAG:-latest}
    container_name: vsuet_rating_v2-fastapi
    env_file:
      - .env
    depends_on:
      - redis
    restart: unless-stopped

  nginx:
    image: nginx:alpine
    container_name: vsuet_rating_v2-nginx
    ports:
      - "80:80"
    volumes:
      - ./front:/usr/share/nginx/html
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - app
    restart: unless-stopped

volumes:
  redis-data:
